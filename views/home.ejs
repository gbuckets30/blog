<%- include('./partials/header.ejs'); -%>

<% blogs.forEach((blog) => { %>
    <h1> <%= blog.title %> </h1>
    <p> <%= blog.content.substring(0, 100) %> ...<a href="/posts/<%= blog.location %>">Read More</a> </p>
<% }); %>

<%- include('./partials/footer.ejs'); -%>
